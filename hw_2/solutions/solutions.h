#ifndef SOLUTIONS_H
#define SOLUTIONS_H

#include <stdlib.h>



int running_total(int a);

int * reverse_in_place(int * a, int len);

int * reverse(int * a, int len);

int num_occurences(int * a, int len, int num);



#endif